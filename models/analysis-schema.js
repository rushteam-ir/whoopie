// Defining schema model
let analysis_schema = new mongoose.Schema({

});

// Defining model statics
analysis_schema.statics = {

};

// Exporting model schema
module.exports = analysis_schema = mongoose.model('analysis', analysis_schema);
